# AGENTS.md —— 「今日热搜」28 天项目协作规则

> **这个文件是给谁看的**：给 AI 助手（WorkBuddy）和未来的我。
> **什么时候读**：每次打开这个工作区的第一件事。
> **为什么第一天就要立规则**：28 天很长，没有规则的项目会跑偏；
> 有了规则，无论哪天、无论换哪个会话，工作方式都一样。

---

## 一、项目信息

| 项 | 内容 |
|---|---|
| 项目名 | 今日热搜（today-hot-search） |
| 一句话定位 | 打开即看、跨平台聚合，帮用户 10 秒判断"今天到底在发生什么" |
| 周期 | 2026-09-16（Day 1）起，共 28 天 |
| 里程碑 | Day 7 上线第一个可用版本（MVP） |
| 工作区 | `C:\Users\lenovo\WorkBuddy\28-day-project`（固定，不用临时会话目录） |
| 远程仓库 | `https://github.com/XingHo-VibeCoding/1.git` |

---

## 二、工作规则（AI 必须遵守）

### 1. 每日清单制
每天开始时，我贴出【今日任务清单】，固定包含九项：
今天要掌握什么 / 今日板块顺序 / 主任务 / 今日不做 / 完成标准 / 今天交什么截图 / 收尾指令 / 余力加练 / 卡住降级。
AI 按清单干活，**清单里没说的事不做**（尤其是"今日不做"里明确禁止的）。

### 2. 一次一步
每个板块做完 → 停下汇报（做了什么 → 结果 → 为什么）→ 等我说「进入下一板块」再继续。
不要一口气把全天任务跑完。

### 3. Git 提交前需确认
- commit 标题格式：`Day X｜一句话说明`
- 说明固定两行：改了什么 / 加了什么
- 每次 commit（和 push）之前：先列出本次改动文件清单，并说明各文件属于哪天的任务，等我确认再动手

### 4. 回退只用 git revert
- 禁止 `git reset --hard`、`git push --force`
- 改错了就 revert，保留完整历史，任何一步都可追溯

### 5. 讲解风格：我是在学习
- 每个操作按三段讲：**做了什么 → 结果是什么 → 为什么要这样做**
- 术语第一次出现必须用大白话解释
- 不只给结论，要给"我怎么判断的"

### 6. 安全红线
- `.env`、密钥、凭据文件永不入仓（`.gitignore` 已挡，提交前仍需复查）
- 不删 `.workbuddy` 文件夹（项目数据，不是缓存）

---

## 三、Day 1 学的两个概念（用自己的话）

### Vibe Coding 是什么
- **传统编程**：人把想法翻译成代码，一行一行写，拼的是语法和记忆。
- **Vibe Coding**：人用大白话说清"我要什么、什么样算好"，AI 负责翻译成代码；人负责**定意图、看结果、做判断**。
- 我的角色变了：从"打字员"变成"验收员"。不用背语法，但要会描述需求、会挑毛病。
- 为什么第一天先立规则：vibe coding 里人不再逐行控制代码，**规则就是新的控制手段**——
  清单制防止 AI 跑偏，提交确认防止误操作，revert-only 保证错了他能退。

### Skill 是什么
- Skill 是**预先写好的操作手册**，教 AI 在特定场景按固定流程干活。
- 类比：给 AI 的菜谱。同样的菜（任务），按菜谱（skill）做，每次都稳定不翻车。
- 和记忆的区别：记忆是"知道发生过什么"，skill 是"知道下次该怎么做"。

---

## 四、当前项目状态（滚动更新）

| 天 | 日期 | 做了什么 |
|---|---|---|
| Day 1 | 2026-09-16 起 | 环境/账号验收（Git 2.54、Node 22、GitHub 账号、WorkBuddy ✅）；建工作区；存本规则文件（注：实际补写于 2026-09-19） |
| Day 2 | 2026-09-17 | git init、.gitignore、index.html 占位页、GitHub上传清单.md；首次提交 8c801f3 |
| Day 3 | 2026-09-19 | 调研 3 个类似产品（tophub.today / 微博热搜 / 知乎待核实）；产出 research.md；commit ebb3918（push 因网络待同步） |

> 每天收尾时更新这一节。
